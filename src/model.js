export const getStatus = () => {
  /*
  var status = 2; // 2 if logged in but no dad profile created.

  var server_url = "http://99.60.8.214:82";

  fetch(server_url + "/user/check_status")
    .then(response => {
      console.log(response.status); 
      return response.json();
    })
    .then(data => {
      // 0 if not logged in.
      if (data === "You must be logged in to use this feature.") {
          status = 0;
      }

      // 1 if logged in and dad profile created.
      else if (data === "You already have a profile created!") {
          status = 1;
      }
    })

   return status; 
   */
}